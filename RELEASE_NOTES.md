# 当前测试状态与已知问题

> MaaHappyFish 仍处于早期测试阶段。

## v0.4.3 更新内容

- **修复 Windows x64 独立发行包 Agent 启动崩溃问题**：
  - **补齐 Python 视觉依赖**：在 Windows x64 嵌入式 Python 打包流程中增补 `opencv-python-headless`，彻底解决脱离开发机环境后 Agent 启动首行 `import cv2` 报 `ModuleNotFoundError` 导致的无法 LinkStart 故障；
  - **建立发布依赖清单规范**：新增 `agent/requirements-release.txt`，规范沉淀嵌入式运行环境核心依赖（`maafw`、`opencv-python-headless`、`numpy`）；
  - **建立 CI 终态实机硬门禁（Hard Gate）**：新增独立 CI 验证 Job `verify (win, x86_64)` 与冒烟脚本 `dev/test_release_agent_imports.py`，直接在 GitHub Actions 真实 Windows 环境下拉起打包产物内部的 `python.exe` 亲自执行 `maa`、`numpy`、`cv2` 及全部 Agent 模块完整导入校验，失败直接熔断阻断 Release 发布；
  - **补充第三方依赖许可证**：在 `THIRD_PARTY_NOTICES.md` 及自动打包发行包中补充 OpenCV Apache-2.0 许可证与项目声明。
- **分发说明**：Windows x64 Release 继续内置 Python 3.13 与 MaaFw，开箱即用。

## 历史版本更新

### v0.4.2 更新内容

- **新增「海獭摸宝」自动化巡检与特定宝石采集 (`SeaOtterGemTask`)**：
  - **支持双启动入口与断点续跑**：严格契合「步骤可恢复导航架构」，支持从「好友水族箱寻宝」列表首页一键进入首位好友，或直接在任意好友鱼缸就地启动断点恢复；
  - **双好友往复摸宝状态机**：在相邻好友（左/右）间交替切换，每次进入好友鱼缸均高效点击海獭摸宝（利用离开即刷新的游戏机制），单次进入零历史数据依赖；
  - **非对称体力耗尽自愈与窗口自适应推进**：
    - 若右侧好友先耗尽，将其作为跳板刷新左侧好友，保障左侧高收益摸宝；
    - 若左侧好友耗尽，自动滑动窗口（`ADVANCE_WINDOW_NEXT`）切入后续好友；
    - 支持级联连续耗尽跳过与防死循环熔断保护；
  - **安全触控与零代币消耗**：锁定左下角海獭安全坐标 `(85, 565)`，远离普通游动鱼群，杜绝误触普通鱼消耗体力；遇到陌生人鱼缸或推荐好友页面自动安全退出；
  - **Maa OCR 正则安全加固**：严格遵循 `std::regex` 规范，杜绝特殊字符引发的 `PipelineChecker` 异常；优化宽 ROI 包络 `[60, 210, 350, 170]` 稳定覆盖体力条与耗尽状态。
- **新增「钓鱼达人」全链路导航与自动钓鱼功能 (`FishingTask`)**：
  - **步骤可恢复导航架构**：支持从自身鱼缸、游乐园大厅或钓鱼达人主页等任意已知阶段就地恢复；
  - **地点参数化自适应**：支持配置 6 大钓鱼地点（初级渔场、海底神殿、珊瑚浅滩、神秘遗迹、沉船宝藏、极地冰原）自动导航到位；
  - **全自动钓鱼状态机 (Phase 2C)**：智能选择普通鱼饵、精准抛竿并进入高帧率咬钩 QTE 监听闭环，零珍贵道具消耗。
- **增强与修复**：
  - 好友摸宝 (`FriendGemTask`) 新增误入“好友鱼宝乐园”自愈分支与单帧气泡漏检有界等待机制；
  - 静态强校验工具 `dev/test_pipeline_regex.py` 纳入日常回归检查。
- **分发说明**：Windows x64 Release 继续内置 Python 3.13 与 MaaFw，开箱即用。

## 历史版本更新

### v0.4.1 更新内容

- **新增「好友水族箱摸宝」自动化巡检与产物采集 (`FriendGemTask`)**：
  - 支持从「我的星级好友」列表首页（顶部第 3 个双人 Tab 处）一键启动，自动点击首位好友进入；
  - 自动定位并快速拾取开阔安全水域中的气泡产物（金币、宝石等）；
  - 达到单缸拾取上限（12 次）或水域拾取完毕后，通过右上角药丸按钮 `>` 快捷无缝切至下一位好友，无需频繁返回列表；
  - **体力耗尽智能自愈与跳过**：检测好友体力状态，遇到已耗尽好友（显示 `0点刷新体力` / `12点刷新体力` 及灰色闪电）时实现 0 点击直接快速跳过；扩大检测 ROI 至 `[60, 210, 400, 140]`，并在控制台实时输出跳过日志；
  - **长尾系统弹窗自愈防护**：巡访过程中遇系统提示弹窗（如“进化鱼特效已被关闭”提示），自动匹配绿色勾选按钮关闭并继续任务；
  - 遍历到达好友列表尽头（加好友界面）或好友数量上限后自动安全结束任务。
- 保持「开贝壳」活动自动化、收鱼产物、海星定时喂食、挂机巡检及鱼食预算独立计算器等功能稳定运行。
- Windows x64 Release 继续内置 Python 与 MaaFw，用户无需另行安装 Python 或 `maafw`。

## 历史版本更新

### v0.4.0 更新内容

- **新增「开贝壳」活动自动化 (`OpenShellTask`)**：
  - 自动连续多轮开贝壳闭环；
  - 普通奖励始终选择“继续开贝壳”；
  - 遇到章鱼自动选择“保留一个随机奖品”；
  - 结算页面自适应包络居中单按钮与偏右双按钮（含分享）两种视觉布局；
  - 采用语义原子 `"随机奖品"` 匹配，彻底解决 PP-OCR 偶发字距切分导致的漏检问题；
  - 支持在任务选项中配置自定义开贝壳轮次（N 次），闭环回到大章鱼活动主界面后自动计数与停止；
  - ⚠️ **前置条件注意**：请在**活动大章鱼开贝壳主界面**开始任务（画面需出现「立即开始」按钮）。
- 保持收鱼产物、海星定时喂食、挂机巡检及鱼食预算提醒等功能稳定运行。
- Windows x64 Release 继续内置 Python 与 MaaFw，用户无需另行安装 Python 或 `maafw`。

## 历史版本更新

### v0.1.0-beta.3 更新内容

- 海星入口改为在前 7 个宠物位置内 OCR 识别“海星”，兼容名称不同的海星，不再依赖固定第 2 个位置。
- 持续实时与巡检收宝模式启动后都会先立即喂食一次，随后再按设定间隔继续；选择“不喂食”时仍不会触发。
- Windows x64 Release 继续内置 Python 与 MaaFw，用户无需另行安装 Python 或 `maafw`。
- 目前仅“持续监测鱼缸并定期补充鱼食”有相对稳定的运行记录，其他功能尚未完成充分测试。
- 已知问题：长时间未查看时，游戏可能退出至模拟器桌面，原始退出原因仍未定位。当前源码已增加静止画面熔断：画面连续 30 秒无明显变化时任务会主动停止，避免 MFA 继续显示“运行中”。