# 公主任务 (`PrincessTask`)

## 功能概述

公主任务是独立的每日领奖流程。任务从主鱼缸识别入口进入，确认公主任务页面后，把右侧奖励列 `[1028, 290, 62, 288]` 分成上、中、下三格依次点击；每格点击命中框并处理领取成功、失败或「积分不足」提示，三格都尝试过后退出回鱼缸。

## 启动契约

`PrincessStartRouter` 使用最深阶段优先：

1. 已在公主任务页面：以 `公主任务_识别.png` 确认后直接开始检查奖励；
2. 在主鱼缸：在 `[29,264,50,37]` 识别并点击 `公主任务_入口.png`，随后必须通过页面门禁；
3. 两种状态均未识别：进入 `PrincessAbort` 安全停止，不点击固定坐标。

## 领取方式与结果分流

整列一次搜索会反复命中最上面、看起来最像模板的礼盒。现将 `[1028,290,62,288]` 均分成三格，每格最多点一次：

- 上格 `[1028,290,62,96]`
- 中格 `[1028,386,62,96]`
- 下格 `[1028,482,62,96]`

点击命中框，不设置固定 `target`。某格没有礼盒或已经点过，就试下一格。命中后进入并列结果分支：

- 成功：在 `[586,518,102,32]` OCR 精确识别并点击“开心收下”；
- 失败：在 `[760,380,180,180]` 识别并点击 `公主任务_领取失败.png`；
- 「积分不足 / 无法领取」：关闭提示后继续下一格。

成功或失败处理后回到公主任务页，再点还没点过的格子。三格都结束后退出。

## 退出与完成

奖励列不再命中后，在 `[1068,121,37,39]` 识别并点击 `公主任务_退出.png`；随后匹配 `主界面特征.png` 确认返回主鱼缸。退出模板或主鱼缸门禁未通过时安全停止。

## 资产与验证状态

使用用户提供的五张模板：`公主任务_入口.png`、`公主任务_识别.png`、`公主任务_领取奖励.png`、`公主任务_领取失败.png`、`公主任务_退出.png`。2026-09-16 按实机未领到奖励的反馈，将三个精确位置改为整列搜索并点击命中位置。
