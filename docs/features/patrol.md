# 多鱼缸巡检

## 目标与边界

`PatrolTask` 是独立于“收鱼产物”的长时间巡检任务：

1. 依次进入鱼缸 1、2、3，收取当前画面中的金币产物；
2. 从第三缸进入鱼缸管理页，并只进入一次通用海星页面；
3. 在通用海星页面依次点击顶部“萌海星 / 乖海星 / 亮海星”标签，为三只海星补充鱼食；
4. 正常完整一轮返回第三缸；若从管理页断点启动，则返回进入管理页前的原鱼缸。验证任一主鱼缸后等待用户配置的巡检间隔，再开始下一轮。

本版本还提供两个默认关闭的每小时子任务：魔力召唤、宝石融合。用户可分别勾选；它们只在一轮多鱼缸收宝与海星喂食结束后的等待阶段运行。

鱼缸 1、2 的海星投放当前限定为廉价鱼食；鱼缸 3 是深海鱼缸，使用弹窗第一格专用鱼食且不触发购买。资源属性及实机测试授权边界见 `docs/game-knowledge.md`。

## 与“收鱼产物”的区别

- `CollectFishTask`：面向当前单个鱼缸，可持续收宝或按占空比激活，并独立定时喂当前缸海星。
- `PatrolTask`：每轮固定巡检 1/2/3 三个鱼缸；收宝结束后只进入一次通用海星页面，再通过顶部标签依次喂食。
- 两个任务入口、计时器和 Pipeline 相互独立，避免改变已有单缸挂机行为。

## 一轮状态机

```text
StartRouter
  -> 归一到鱼缸 1
  -> 收宝 1（连续 30 秒无目标才结束；每次命中后重新计时）
  -> 识别并切换鱼缸 2 -> 验证 -> 收宝 2
  -> 识别并切换鱼缸 3 -> 验证 -> 收宝 3
  -> 识别扳手 -> 验证鱼缸管理页 -> OCR 点击任一海星进入通用海星页面
  -> OCR 点击“萌海星”顶部标签 -> 验证正文标题 -> 按实际喂食弹窗选择鱼食
  -> OCR 点击“乖海星”顶部标签 -> 验证正文标题 -> 按实际喂食弹窗选择鱼食
  -> OCR 点击“亮海星”顶部标签 -> 验证正文标题 -> 按实际喂食弹窗选择鱼食
  -> OCR 点击通用返回 -> 验证鱼缸管理页 -> 再返回并验证任一已知主鱼缸
  -> WaitLoop（主巡检 > 魔力召唤 > 宝石融合；全局弹窗仍可抢占）
```

收宝识别范围为 `[0, 100, 1280, 560]`，覆盖横向水体但排除顶部资源栏。金币模板命中后点击识别结果中心，不设固定坐标；每次点击后立即在鱼缸底部安全范围 `[201,630,826,66]` 内执行 `(221,663) -> (1007,663)` 左到右滑动，主动收取空中产物后再继续识别。每个鱼缸使用硬编码的 30 秒“无产物窗口”：窗口内一旦命中并点击产物就重新开始 30 秒计时；连续 30 秒没有识别到产物（包括一开始就没有、或已经全部收完）时视为该缸正常完成并切换下一缸，不记录为任务错误。该时长不暴露为用户参数。

## 计时语义

- 可配置项：`多鱼缸巡检间隔`，默认 30 分钟。
- 任务启动先立即执行一轮。
- 首轮结束后才初始化等待计时，避免首轮结束时立即重复执行。
- 等待期间每秒回到调度循环一次，因此跨零点签到和特惠礼包全局弹窗仍能被处理。
- 魔力召唤和宝石融合使用彼此独立的 3600 秒计时器；勾选后在首轮主巡检完成时立即检查一次，此后每小时检查。
- 若多个任务同时到期，固定优先级为：多鱼缸收宝与喂食 > 魔力召唤 > 宝石融合。

## 日志与等待心跳

- 巡检的关键动作和识别结果通过 Pipeline `focus` 写入 MFA 日志面板，同时进入客户端日志文件。日志按“当前对象 -> 执行动作 -> 验证结果/下一步”表述，不要求用户从内部节点名判断进度。
- 三缸收宝分别打印：进入哪个鱼缸、发现产物并完成底部滑动、30 秒无产物窗口重新计时、连续 30 秒无产物后完成该缸并切换。
- 三只海星分别打印：点击并确认哪个标签、打开选择鱼食、识别为普通或深海专用鱼食、执行投放、弹窗关闭且正文仍为当前海星时确认投放成功。
- 每轮完成后打印下一次主巡检的绝对时间；静止等待期间每 60 秒打印一次心跳和剩余秒数。屏幕不动但心跳继续出现表示正常等待；门禁失败会打印明确的安全停止日志，不会伪装成等待。
- 魔力召唤区分“高级召唤已启动”“上一轮结果已揭晓并领取”“检测到正在召唤，本次未重复召唤”；宝石融合区分“上一轮结果已放入仓库并再次合成”“已启动下一轮”“检测到正在融合，本次未重复合成”。两项完成后都打印已返回主鱼缸并继续调度。
- 等待间隔从每轮主巡检真正完成时重新计时，不把执行本轮巡检所花的时间计入下一轮等待。

## 通用海星页面与深海鱼食兼容

- 从任一鱼缸的管理页进入海星后，实际都会进入同一个通用海星页面。流程只进入一次，随后在覆盖三个标签的顶部 ROI `[80, 70, 800, 100]` 依次 OCR 点击“萌海星 / 乖海星 / 亮海星”，并在正文 ROI `[330, 140, 420, 130]` 用对应完整名称确认切换成功。禁止为了喂下一只海星退出页面、切换鱼缸后再重新进入。
- 鱼缸与海星的对应关系不硬编码。当前鱼缸 3 恰好对应第二个“乖海星”，但以后对应关系可能变化；每只海星打开喂食弹窗后，都先按弹窗的真实内容路由：匹配到 `普通鱼食袋.png` 就点击廉价鱼食，匹配到深海加号才进入专用鱼食分支。
- 在选择喂食弹窗中，用用户指定范围 `[457, 216, 189, 186]` 匹配 `海星_加号.png`，以此确认已经进入正确弹窗。
- 通过门禁后点击用户指定的第一格专用鱼食范围 `[579, 210, 196, 195]`。这是唯一允许的固定目标区域；若加号门禁未通过，不点击鱼食并安全返回。
- 点击后先在 `[700, 90, 240, 150]` OCR 检查“选择喂食”：若弹窗仍在则安全停止；若弹窗已关闭，则必须重新识别当前海星正文标题后才能切换下一个标签。不能把候选节点的 `on_error` 当作“未识别即成功”分支。
- 不购买深海专用鱼食。`海星_加号.png` 是候选复用模板，仍需在鱼缸 3 真实弹窗中完成 Maa 实机匹配验证。

## 每小时可选子任务

### 魔力召唤

```text
右下角宝箱 [1120,562,141,141]
  -> 魔力召唤入口 [415,593,173,124]
  -> OCR 标题 [468,0,345,146]
  -> 上一轮刚结束：OCR 点击“揭晓” [827,501,210,145] -> OCR 点击“确定” [538,399,202,142] -> 接空闲启动流程
  -> 有高级召唤黄色按钮 [811,495,232,148]：点击 -> 绿色勾选 [751,404,153,150] -> 返回
  -> 无黄色按钮：视为正在召唤 -> 直接返回，不报错
```

标题 OCR 同时兼容整体“魔力召唤”和分词后的“魔力”“召唤”。页面状态按并列分支处理：高级召唤确认弹窗优先，其次是上一轮结束的“揭晓”，再其次是空闲状态的高级召唤按钮；以上均未命中但仍在召唤页时，视为正在召唤并直接返回。揭晓后必须在用户给定 ROI 内 OCR 命中“确定”才点击，随后接回既有空闲启动流程。高级召唤会消耗稀缺资源且十小时才能再次执行，确认前必须再次核对页面门禁。

### 宝石融合

```text
右下角宝箱 [1120,562,141,141]
  -> 宝石融合入口 [632,591,135,128]
  -> 若出现结果弹窗：OCR 放入仓库 [498,486,283,138]
  -> 验证宝石融合页面 [549,0,178,242]
  -> 有“合成” [825,556,178,140]：点击后返回
  -> 无“合成”：视为正在融合 -> 直接返回，不报错
```

结果弹窗、空闲可合成和正在融合三种路由都不会使用识别失败后的盲点坐标。

## Start Contract

支持从以下真实页面启动或恢复：

- 鱼缸 1、2、3 主页面：识别左上角当前鱼缸编号；若不是第一缸，先通过展开选择器归一到第一缸。
- 已展开的鱼缸选择器：识别并点击选择器中的第一缸。
- 鱼缸管理列表页：按 deepest-first 直接进入通用海星页面，不强制退回主鱼缸。退出管理页后接受真实返回的 1/2/3 缸，下一轮再归一到第一缸。
- 通用海星页面：从“萌海星”开始重新执行三标签喂食流程。若任务从“选择喂食”弹窗启动，由于无法可靠知道上一步所属标签，安全停止而不盲点。

其他未知页面不做固定坐标兜底，直接 `StopTask` 并输出门禁失败日志。

## 模板来源与复核

本功能采用 ADB 探索模式。用户提供的三张截图仅用于说明业务流程，没有直接作为模板。

主 Agent 在 2026-09-08 通过 maa-mcp 连接 MuMu，逐页确认并审核以下固定 UI 模板：

- `image/patrol/鱼缸1_主页面编号.png`（鱼缸 1 主页面编号）
- `image/patrol/鱼缸2_主页面编号.png`（鱼缸 2 主页面编号）
- `image/patrol/鱼缸3_主页面编号.png`（鱼缸 3 主页面编号）
- `image/patrol/鱼缸1_入口.png`
- `image/patrol/鱼缸2_入口.png`
- `image/patrol/鱼缸3_入口.png`
- `image/patrol/鱼缸1_管理页未选中.png`
- `image/patrol/鱼缸2_管理页未选中.png`
- `image/patrol/鱼缸3_管理页未选中.png`
- `image/patrol/鱼缸1_已选中.png`
- `image/patrol/鱼缸2_已选中.png`
- `image/patrol/鱼缸3_已选中.png`
- `image/patrol/鱼缸管理_扳手.png`

裁图仅保留固定编号牌、选中外圈或扳手按钮，不包含鱼、倒计时、容量数字等动态内容。

本轮新增子任务严格使用用户已提供的数据，而不是重新猜测坐标：`右下角_宝箱.png`、`魔力召唤入口.png`、`点击高级召唤.png`、`绿色勾选按钮.png`、`宝石融合入口.png`、`宝石融合页面.png` 以及上文列出的 ROI 已逐项写入 Pipeline，并由静态测试锁定。用户提供的说明截图用于确认页面关系；现有紧裁模板用于实际匹配。

### 模板取证事故与约束

初版误把“展开选择器里的小号鱼缸编号”同时用于识别“主页面左上角的大号当前鱼缸编号”。两者肉眼图案相似，但真实像素尺寸不同，导致主页面启动识别 8/8 次失败。改用完整主页面编号牌后又通过交叉实机检查发现：三张图共享区域过多，错误编号也可能超过低阈值。最终模板进一步紧裁到数字本体，并用三个主页面互作负样本。以后必须遵守：点击或识别哪个页面状态下的控件，就从该页面状态单独截图；主页面当前缸标识、展开选择器入口、管理页选中态不得互相代用。每类模板接入前都要在其真实页面用 Maa benchmark_node 验证，并对同类相似控件做交叉负样本验证。

## 当前验证状态

- 已修复：主页面 1/2/3 缸分别使用从各自主页面截取的数字本体模板；真实帧回归同时要求目标鱼缸命中、另外两个鱼缸不得命中。
- 已实机启动：从鱼缸 1 主页面运行真实 `PatrolTask`，入口以 0.950 分数识别鱼缸 1 并进入收宝阶段；原先“主页面未识别”的失败已不再复现。
- 已修复：展开选择器和鱼缸管理页分别使用各自页面真实截图；三个相似编号不再共享宽 ROI，而是分别限制到各自真实按钮区域。展开选择器已实机正确切换 1→2→3→1；管理页未选中模板均为实机 1/1 命中，选中态验证中 2/3 缸均连续 3/3 命中。
- 已完成：加入主页面与展开选择器真实帧回归夹具，静态测试会验证各模板只能在对应页面状态和对应按钮区域命中；加入每缸 30 秒无产物窗口的拓扑测试。
- 已实机串联：从 2 缸开始，连续 30 秒无产物后正常切到 3 缸；3 缸点击两枚产物后重新计时，再连续 30 秒无产物后正常进入鱼缸管理页，全程未把“无产物”当作错误。
- 已完成：JSON/正则、Agent 注册引用、计时器单元测试、接口同步和发布 Agent import 冒烟测试。
- 已完成代码：鱼缸 3 动态选择当前缸海星、加号门禁后点击第一格深海专用鱼食；魔力召唤和宝石融合独立每小时调度、可选开关及固定优先级。
- 2026-09-09 用户 MFA 实测确认：三缸收宝与 30 秒无产物切缸正常；旧喂食流程错误地三次进入同一个“乖海星”，且深海鱼食实际点击成功后因负向候选分支写法错误而超时、退出重进。代码已改为单次进入通用页面、三标签逐项验证、根据实际弹窗动态选择普通/深海鱼食；修复后的完整喂食流程待下一次 MFA 实测。
- 已完成代码验证：20 项巡检/计时器测试、125 个 Pipeline 正则双层校验、全部 CustomAction/CustomRecognition 注册引用门禁通过。
- 2026-09-11 已完成代码：为三缸收宝、三只海星喂食、魔力召唤、宝石融合与返回调度补齐结果日志；等待阶段增加下一轮绝对时间和 60 秒心跳，并让后续每轮在完成后重新开始计算等待间隔。专项巡检/计时器测试已覆盖日志语义、互斥结果分支、心跳节流与重复轮次计时；本次按用户要求未启动模拟器。
- 待 MFA 实测：鱼缸 3 的 `海星_加号.png` 候选复用模板与第一格专用鱼食投放；完整一轮三缸喂食、返回与第二轮唤醒。
- 待 MFA 实测：魔力召唤空闲启动并确认、正在召唤安全返回、上一轮结束后“揭晓 -> 确定 -> 开启下一轮”；宝石融合空闲启动、结果入库后重启、正在融合安全返回。魔力召唤结果分支已按用户手动提供的文字与 ROI 完成代码接入，不能标记为实机通过。
- 待观察：30 秒无产物窗口是否足以覆盖鱼群遮挡；若实测漏收，只调整窗口时长，不增加盲点坐标。

### MaaMCP 长任务测试注意

`PatrolTask` 设计为进入 `PatrolWaitLoop` 后长期运行，不应把 maa-mcp 的 `run_pipeline` 当作会自然结束的短测试。2026-09-08 的调试中，工具调用超时后 `stop_pipeline` 返回过 `tasker_stopped=false`，旧循环仍在日志中继续运行；当次已核对 PID 并终止 MaaMCP 进程完成清理。后续实机调试必须采用有界节点/阶段测试，或在外部明确观察到目标阶段后立即停止，并核对停止结果，不能只看调用返回就假定后台任务已结束。
