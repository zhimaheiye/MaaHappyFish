# 全局页面处理：活动页面、每日签到、特惠礼包、快报与升级弹窗

## 功能目标

签到窗口会在跨过零点后由游戏主动弹出，特惠礼包、活动页面、快报或升级弹窗也可能在任务运行期间出现。自动化任务不主动寻找入口，而是在所有标准 `1280×720` 游戏 Pipeline 状态转换检查点优先识别并处理这些页面。

## 用户截图契约

本功能采用用户手动截图模式，以下模板按用户说明直接接入，不重新截图或复核其内容：

- `活动页面_退出.png`：用户确认该模板表示活动页面已经弹出；在 `[0, 0, 136, 116]` 内识别，点击模板实际命中位置后返回鱼缸主页面；
- `签到_识别.png`：确认签到弹窗已经出现，只用于识别，不点击；
- `签到_点击.png`：下方「签到」二字，只在屏幕下半区 `[0, 400, 1280, 320]` 内匹配并点击实际命中位置；标题「签到寻宝」不在此 ROI 内。模板未命中时，再用 OCR `^签到$` 在同一 ROI 内点击文字。
- `签到_关闭.png`：领取后关闭签到页面的按钮；只在签到动作成功后识别，ROI `[1000, 0, 220, 160]`，点击模板命中框。不再使用固定 6×6 绝对坐标，避免关不上后又反复点「签到」。
- `特惠礼包_识别.png`：确认特惠礼包弹窗已经出现，只用于识别；
- `特惠礼包_关闭.png`：特惠礼包关闭按钮，只通过模板匹配结果点击。
- 快报页面先在 `[635,23,226,162]` OCR 识别“快报”作为页面门禁；
- `快报页面_关闭.png`：门禁通过后在 `[1044,77,26,31]` 匹配并点击关闭按钮的实际命中位置。
- `升级弹窗_识别.png`：在 `[200,348,378,185]` 内确认升级弹窗已经出现，只用于页面门禁；
- 门禁通过后在 `[957,627,125,47]` OCR 识别“太好了”，点击 OCR 实际命中位置关闭弹窗。

流程中没有固定坐标点击。

## 状态分支

```text
任意 Pipeline 检查点
  └─ 识别到签到弹窗（标题「签到寻宝」，不点击）
       └─ 在屏幕下半区识别并点击「签到」二字
            ├─ 识别到关闭按钮：点击关闭
            └─ 签到弹窗已自动消失：直接完成
       └─ [JumpBack] 返回原任务继续扫描
```

`GlobalDailySignClose` 和 `GlobalDailySignAutoClosed` 是并列分支，不要求领取后一定出现关闭按钮，也不把自动关闭视为错误。关闭识别绝不能放到领取之前：只有一层签到页面，先关闭会漏签。领取必须点弹窗**下方**的「签到」二字，不能点标题，也不能在未领取时点右上角关闭。

特惠礼包流程为单一路径：识别 `特惠礼包_识别.png` → 识别并点击 `特惠礼包_关闭.png` → 回到鱼缸。用户已确认关闭后没有额外步骤。

活动页面流程为单节点路径：优先识别 `[0, 0, 136, 116]` 内的 `活动页面_退出.png` → 点击实际模板位置 → 等待返回鱼缸 → 通过 `[JumpBack]` 继续原任务。该处理不使用固定点击坐标。

快报流程为两段页面门禁：在 `[635,23,226,162]` OCR 命中“快报” → 在 `[1044,77,26,31]` 匹配 `快报页面_关闭.png` 并点击实际位置 → 通过 `[JumpBack]` 回到刚才的任务节点继续执行。未命中“快报”时绝不单独点击右上角关闭区域。

升级弹窗同样采用两段门禁：在 `[200,348,378,185]` 匹配 `升级弹窗_识别.png` → 在 `[957,627,125,47]` OCR 命中“太好了”并点击实际文字位置 → 通过 `[JumpBack]` 回到刚才的任务节点继续执行。未确认升级弹窗时绝不点击确认区域。

该通用模板不用于大章鱼开贝壳页面；开贝壳页必须先用 `开贝壳_识别.png` 确认页面本体，再用 `贝壳页面_返回.png` 识别并点击返回按钮。

## 全局接入边界

- 除“点击产物气泡后立即滑动收取”的原子链外，20 份标准 `1280×720` 游戏 Pipeline 的所有非空 `next` 列表均把 `[JumpBack]GlobalActivityPagePopup`、`[JumpBack]GlobalDailySignPopup`、`[JumpBack]GlobalSpecialOfferPopup`、`[JumpBack]GlobalNewsPopup` 和 `[JumpBack]GlobalLevelUpPopup` 放在业务节点之前，因此全局页面会优先于正常后继节点处理；原子链会在紧接着的下一个检查点处理弹窗，避免破坏双倍收取手势；
- `features/mobile_ads.json` 与 `features/emulator_ads.json` 都会进入外部广告画面，分别使用手机 `1600×720` 和模拟器 `1280×720` 的独立资产；两者不接入游戏签到/活动等全局门禁，防止在广告内容中误识别、误点击；
- 收鱼挂机的无限循环同样接入，适合跨零点长时间运行；
- 正在执行中的单个原子动作不会被强行打断；如果弹窗恰好在该动作期间出现，会在动作结束后的下一个 Pipeline 检查点处理，避免后台并发点击和误触；
- 独立运行 `DailySignTask` 时，当前没有签到弹窗也会正常完成，不报错。

## 待实机验证

- [ ] 领取后弹窗自动关闭分支；
- [ ] 领取后需要点击右上角关闭按钮分支；
- [ ] 跨零点长时间挂机时，处理完成后原任务能够继续。
- [ ] 特惠礼包在各业务中途出现时能够关闭并回到鱼缸；关闭动作本身已由用户手动确认，但尚未完成 MFA 自动化实测。
- [ ] 快报在各业务中途出现时能够识别、关闭并通过 JumpBack 继续原任务。
- [ ] 升级弹窗在各标准游戏业务中途出现时能够识别、点击“太好了”并通过 JumpBack 继续原任务。

用户当前无法确认领取后窗口究竟会自动关闭还是需要手动关闭，因此两条分支都必须长期保留，不能在未经实测的情况下删掉其中之一。

2026-09-09 日志确认领取按钮已成功点击，关闭模板也以 `1.000` 命中，但默认随机点击落在匹配框左上缘，页面未关闭后又被全局处理器重新识别，造成反复点击签到。2026-09-16 再次出现只反复点「签到」、不点右上角关闭：固定点 `[1103,63,6,6]` 容易偏离真实 X。关闭改为点击 `签到_关闭.png` 命中框；下方「签到」点不到时也直接去关窗。

2026-09-16 用户反馈签到弹窗出现后只关了窗、没有点下面的「签到」二字。领取识别改为仅在下半区 `[0, 400, 1280, 320]` 匹配 `签到_点击.png`，未命中则 OCR `^签到$` 点击文字；标题「签到寻宝」不再参与领取点击。未点到下方签到时不会进入关闭。

特惠礼包关闭后会回到鱼缸，可能丢失原业务页面。当前全局处理保证识别与关闭；各任务能否从该时点继续，取决于原任务当前节点的步骤可恢复能力，后续 MFA 测试应重点观察这一点。

2026-09-11 已按用户提供的 `活动页面_退出.png` 与 ROI 完成全 Pipeline 接入。

2026-09-13 已按用户说明新增“快报”OCR 页面门禁与 `快报页面_关闭.png` 点击节点，并完成当时所有标准游戏 Pipeline 的状态转换检查点接入；专项测试、正则、资源加载和引用门禁通过。本次未做模拟器测试。

2026-09-14 已按用户提供的 `升级弹窗_识别.png` 与两组 ROI 新增升级弹窗全局处理，并将 `[JumpBack]GlobalLevelUpPopup` 接入 20 份标准游戏 Pipeline 的 348 个状态转换检查点；手机与模拟器两份外部广告流水线保持隔离。本次只做代码级验证，不做模拟器测试。
