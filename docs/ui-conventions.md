# 游戏通用 UI 识别约定

## 通用返回按钮

- 适用语义：用户后续提到“通用的返回按钮”时，指游戏页面左上角带“返回”文字的按钮。
- 识别方式：OCR。
- `expected`：`返回`。
- `roi`：`[1, 0, 189, 119]`（基准画面 `1280×720`）。
- 动作：点击 OCR 实际识别结果；禁止设置固定坐标 `target` 作为识别失败后的兜底。
- 验证边界：这是用户提供的通用约定，目前随驯鹿鱼功能首次接入；是否适用于具体新页面，仍应由对应功能的 MFA 实测结果确认。
