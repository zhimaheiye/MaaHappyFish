# 待修复问题记录 (ISSUES.md)

本文档只保留尚未完成的问题。已完成事项会迁移到对应功能文档、`PROJECT_STATUS.md` 与 `docs/handoff/CURRENT.md`。

## 当前待修复

（暂无）

## 2026-09-24 已完成批次

- 全仓独立任务缺少中途启动导航：已建立 `docs/features/start-navigation-audit.md` 并补齐深海寻鱼、开贝壳、单次海星、公主任务、金海豚、乐队鱼和摇一摇结算的有证据恢复入口。摇一摇局内因缺少可区分模板保留为 Live Blocked；乐队鱼好友邀请弹窗与通用选择喂食弹窗因缺失运行时身份上下文故意不支持。专项、正则/资源加载、Agent 引用、更新契约与 Python 编译通过，本轮未操作模拟器、ADB 或 maa-mcp。
- 鱼宝乐园接入后出现全局“资源加载失败”：MaaFramework 日志明确定位到 `features/fish_baby.json` 无法打开。根因是 `FishBabyVerifyTankAfterRound.on_error` 数组缺少右方括号，导致 JSON 非法；现已补齐。专项资源检查已确认 31 份 Pipeline 的 228 个正则均可编译，MaaFramework 成功加载 764 个节点；本次未操作模拟器或执行整任务回归。
- 日常收尾的金海豚在实际已出现“游戏结束”弹窗时仍把 `GoldenDolphinPlayGame` 判失败。现场日志显示动作恰在正式阶段满 45 秒时返回失败，200 ms 后保存的错误截图已是结算页；既有取消模板在该截图上匹配分数为 `0.999`，排除模板失效。现改为 45 秒后停止奖励点击并逐帧等待结算页，最多保留 15 秒结算动画余量；缺模板或余量耗尽仍安全停止。本次未操作模拟器或运行测试套件。
- 日常收尾的乐队鱼首次检查停在主鱼缸并于 20 秒后失败：`BandFishStartAtTank` 使用 OCR 识别左下角艺术字“游乐园”，现场始终未命中。随机出现的鱼缸提示气泡与故障无关。现改为复用 `游乐园入口.png` 模板，在本次失败截图的指定 ROI 内匹配分数为 `0.998`；只点击真实模板命中位置，再进入既有游乐园面板门禁。本次未操作模拟器或运行测试套件。

## 2026-09-23 已完成批次

- 鱼宝乐园此前因缺宝宝编号到点击中心、绿勾、睡眠、结算和退出证据而停在首次资源消耗前。2026-09-23 已用 Maa MCP 补齐全链路原图与坐标，以数字模板动态映射当帧红旗编号，接入红心阶段恢复、睡眠跳过、三步分组养成、执行按钮文字门禁、冷却确认和两级退出回缸；单宝宝实测 `33/42 → 38/42`，消耗 15 鱼食和 100 爱心，开心宝 0。按用户要求未运行测试套件或整任务 MFA 回归。
- 秘境之门旧版会在送出弹窗延迟时把订单记为“无响应”并跳过，且以旧模板代替鱼苗数量判断。现已删除 `SecretRealmGatePopupWait`、`SecretRealmGateMarkNoResponseAction` 和 skip-row 状态；改为 9 秒 fresh-frame 观察、选择面板 N/M 判定、无鱼/鱼苗不足同卡删除、领奖动画后等待确定，并屏蔽钞票付费锁定单。功能保留代码但从独立任务、默认预设和日常多选 UI 隐藏；离线专项与静态门禁通过，本轮未做 MFA/模拟器测试。
- 乐队鱼独立任务邀请后等待好友接受：`PENDING` 回访改为每次在主鱼缸可取消地等待 60 秒，最多等待 6 分钟；每次重进沿既有 `BandFishStatusRouter` 先检查“开始演出”，到期仍未就绪时输出明确错误。`dev/test_band_fish_pending.py` 与通用门禁完成代码级验证，本次未做 MFA/模拟器测试；实机等待与演出闭环保留为独立验证项。

## 2026-09-19 已完成批次

- 金海豚 Candidate Fix 已完成代码闭环：启动阶段每帧只消费一个 fresh activation coin 坐标；正式阶段使用 priority-first + fallback-fill，单帧总点击预算为 4；普通“想玩”弹窗跳过 RapidOCR，并以“点击 -> fresh 截图 -> confirm 模板复核”的最多三次闭环确认进入；无红 X 的疑似耗尽分支才 lazy 加载 RapidOCR 作辅助判断。`dev/test_golden_dolphin_pipeline.py`（含 12F/12G/12H）及公共门禁均通过，本次未做 MFA/模拟器测试。后续自然实机验收保留在 `docs/handoff/CURRENT.md`，不再作为待修复 Bug。
- 驯鹿鱼“一键清除”误触已完成代码闭环：`ReindeerFishClearAll` 从全部正常 `next` 路由移除，节点动作改为 `DoNothing`，收取后只处理奖励返回/一键回礼，未命中时走通用返回。专属场景测试与日常调度器契约均通过；收取/回礼后的完整业务链继续作为 MFA 验证项保留，不再作为待修复 Bug。

## 2026-09-16 已完成批次

- 收鱼 SHAKE 挂机：v0.4.4 现场日志显示 MuMuManager shake RPC 连续 2 秒超时后熔断，两次把「收鱼产物」整任务判失败。已将挂机 RPC 超时放宽到 5 秒；连续 3 次失败改为跳过本轮剩余摇晃并扫底，然后继续挂机。摇一摇小游戏本身仍保留硬熔断。
- 兑换金贝壳券：实机点击「兑换」后出现消耗确认弹窗（绿色对号），此时右上角兑换按钮仍在是正常的。旧逻辑把「按钮还在」当成未生效并停任务。已改为先点对号，再点「太好了」`[568,512,153,51]`，然后两次返回鱼缸。接入 `金贝壳_识别.png` `[515,360,232,195]` 作为主页门禁；误入海星宠物页时先点返回再重试。
- 每日签到：弹窗出现后只关了窗、没点下方「签到」二字。领取改为只在屏幕下半区点「签到」，模板未命中则 OCR 点这两个字。后又出现反复点「签到」却不点右上角关闭：关闭改为点击 `签到_关闭.png` 命中框，不再使用固定 6×6 坐标。
- 日常收尾开贝壳：绿野寻仙踪日常复用开贝壳时停在分类页（普通贝壳/金贝壳）报门禁失败。已放宽左侧「进入」ROI，分类页可恢复；进不去大章鱼页则点「返回」回鱼缸，再走后续买鱼。
- 购买廉价鱼食：已在详情页仍报「页面门禁未通过」。现场日志是点进详情后数量框 OCR 失败（ROI 偏大读到单价 400）。已收紧数量 ROI，并避免把详情页误判成商品列表。
- 公主任务：三个精确奖励框未领到礼盒。改为在 `[1028,290,62,288]` 整列搜索 `公主任务_领取奖励.png`，点击命中位置后再处理开心收下/领取失败。
- 新增绿野寻仙踪独立任务，以及日常收尾子任务「绿野寻仙踪日常」（开贝壳 1 次 + 商店买一条贝币鱼）。独立任务点进活动后因「任务」切图与 ROI 一样大未命中，约 23 秒 Abort 停在任务页；已放宽 ROI、增加 OCR「任务」兜底，已在任务页则直接一键领取，没有领取按钮也返回鱼缸。
- 驯鹿鱼送收礼物：今日礼物已送完页只有「一键清除」，旧流程曾补充 OCR 点击后返回；2026-09-19 已按“绝不自动清除好友送礼记录”的业务规则废止该行为，改为忽略一键清除并走通用返回。
- 海獭摸宝：会提前结束并漏清多名好友体力。不是深海缸问题。点赞模板未命中时，「下一位」会把真好友当成推荐玩家并 `DONE`；海獭点击还写死在 `(85,565)`，实际海獭常在更右侧。已改为先认体力栏「剩余/刷新体力」，并点击海獭模板命中框。
- 挂机到点插入：收鱼/巡检/好友摸宝可在 12:00 插入日常收尾（默认全选、每日一次），收鱼/巡检可在 10:00 与 22:00 插入好友摸宝兜底（默认关）。失败兼容（子任务 StopTask 会停掉整段挂机）后续再补。

## 2026-09-15 已完成批次

- 收宝滑动：所有点击/摇动后的鱼缸底部收取均改为左到右、再右到左的双向往复滑动。
- 浪漫满屋：气泡轮询频率调整为 200ms，遮挡等待由 6 秒延长至 30 秒，超时后仍按页面门禁重试珊瑚入口。
- 钓鱼达人：甩杆/等待动作失败改由结算路由接管并有界等待；退出改为钓场、地点页、主鱼缸三段状态确认；日常普通饵食仍为 5 杆，美味饵食模式不限杆数并由用户手动停止。
- 金海豚：隐藏启动阶段只识别无编号 `金海豚_贝币.png`；检测到其他奖励后切换到四类可选优先级连续点击；游戏动作必须等到真实结算页才进入退出，修复奖励仍在下落时提前退出报错。
- 日常收尾：补齐初始化选择/跳过摘要、各子任务开始日志与摇一摇流转日志，并加入可选“宝石订单”子任务。
- 宝石订单：确认回到主鱼缸后由独立完成动作按日常/独立模式分流，修复独立运行结束后落入错误路由。
- 手机看广告：新安装默认任务列表不显示，仍可从“添加任务”加入；界面说明明确 ADB 真机、仅适配 2400x1080 到 1600x720 与不支持 MuMu 等限制。
- 客户端图标：发布安装脚本将项目 `happyfish.ico` 写入 `Assets/logo.ico`，工作流纳入图标变更触发。
- 多鱼缸巡检：确认等待心跳已按 5 分钟节流，文档与契约状态同步关闭。

以上事项均已完成代码级验证；本批次按用户要求未做 MFA、模拟器或真机测试。
